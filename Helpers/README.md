# Helpers

Design assets and reference material for EduCollab (not part of the API runtime).

| Path | Purpose |
|------|---------|
| `design-tokens.md` / `index.css` | Email and UI token reference (used by `EduCollabEmailTemplates`) |
| `*.png` | Diagrams and mockups |

**API contract:** use [`openapi/v1/openapi.json`](../openapi/v1/openapi.json) and [`EduCollab.Api.postman_collection.json`](../EduCollab.Api.postman_collection.json), not this folder.

Regenerate those with `scripts/export-openapi.ps1` and `scripts/export-postman.ps1`.
